{
  "name": "@pickly/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src prisma/seed",
    "test": "echo \"database: tests run via api integration suites\"",
    "generate": "prisma generate",
    "migrate:dev": "dotenv -e ../../.env -- prisma migrate dev",
    "migrate:deploy": "dotenv -e ../../.env -c -- prisma migrate deploy",
    "migrate:reset": "dotenv -e ../../.env -- prisma migrate reset --force",
    "seed": "dotenv -e ../../.env -- tsx prisma/seed/index.ts",
    "studio": "dotenv -e ../../.env -- prisma studio"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.0.1"
  },
  "devDependencies": {
    "@pickly/tsconfig": "workspace:*",
    "@pickly/eslint-config": "workspace:*",
    "@pickly/contracts": "workspace:*",
    "prisma": "^6.0.1",
    "dotenv-cli": "^7.4.4",
    "typescript": "^5.6.3",
    "eslint": "^9.14.0",
    "tsx": "^4.19.2",
    "@types/node": "^22.9.0"
  }
}
